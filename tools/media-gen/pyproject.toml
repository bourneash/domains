[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "media-gen"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "fastapi>=0.110", "uvicorn>=0.29", "pydantic>=2.6", "filelock>=3.13"]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.23"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
markers = ["live: hits real ComfyUI/CloakBrowser (opt-in)"]
addopts = ["-m", "not live"]

[tool.setuptools.packages.find]
where = ["src"]
