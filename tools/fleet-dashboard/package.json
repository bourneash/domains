{
  "name": "fleet-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio fleet dashboard — engineer liveness, git status, and task-board CRUD across the domains repo",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "NODE_ENV=test node --test"
  },
  "dependencies": {
    "cronstrue": "^2.59.0",
    "express": "^4.19.2",
    "js-yaml": "^4.1.0"
  }
}
